
<button class="button" style="margin-left: -500px;"><%= link_to 'Back', user_path(@user) %></button> <h3>All Restaurants in Terminal <%= @terminal.number %></h3>
<br/>
<% @terminal.restaurants.each do |rest| %>
    <% if rest.name != "George Bush Intercontinental Airport"%>
    <%= link_to rest.name, restaurant_path(rest) %>
    <%# <%= rest.name %>
    <br/>
    <% end %>
<% end %>



